OPNSENSE_BASE_URL=https://opnsense.example.local
OPNSENSE_API_KEY=replace-me
OPNSENSE_API_SECRET=replace-me
# Set to true only when using self-signed TLS in local development.
OPNSENSE_INSECURE=false
# Optional path to a CA bundle for OPNsense TLS verification.
OPNSENSE_CA_CERT=/path/to/ca.pem
